<!DOCTYPE html>
<html>
  <head>
    <title>Thatsite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

  <body>
      <div class="flex-container">
        <%= render "member/shared/sidebar", site: @site, other_sites: @other_sites%>
        <div class="main">
          <%= yield %>
        </div>
      </div>
  </body>
</html>
